#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    int i, j;
    char str[100];
    printf("Enter a string : ");
    gets(str);
    printf("\t%d\n", strlen(str));
    for (i = 0; i != strlen (str); i++)
    {
        if (str[i] == '>')
        {
            // str[]  
        }
    }
}